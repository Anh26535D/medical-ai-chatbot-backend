# App configuration
PORT=8080
GIN_MODE=release

# Database connections
MONGODB_URI=mongodb://mongodb:27017
REDIS_ADDR=redis:6379

# MQTT Broker connection
MQTT_BROKER_URI=tcp://emqx:1883

# Firebase RTDB Integration (Production credentials)
FIREBASE_DATABASE_URL=https://your-firebase-project-default-rtdb.firebaseio.com/
# Path (inside the container) to the Firebase service account JSON used both for
# admin-level RTDB REST calls and for minting Firebase Auth custom tokens at login.
# Mounted via docker-compose.yml as ./firebase-adminsdk.json:/app/firebase-adminsdk.json:ro
FIREBASE_KEY_PATH=/app/firebase-adminsdk.json

# JWT authentication security secret
JWT_SECRET=your_jwt_secret_key_here

# Shared secret the backend's own MQTT worker uses to authenticate with EMQX
# (see internal/worker/mqtt_worker.go and MQTTWorkerClientID in internal/repository/mqtt_worker_identity.go)
MQTT_WORKER_SECRET=your_mqtt_worker_secret_here

# EMQX Management API - lets the backend forcibly disconnect a device's live MQTT
# session on unpair (internal/repository/emqx.go), so a revoked access token takes
# effect immediately instead of waiting for the session to drop on its own.
# Create with: emqx ctl admins add <user> <pass> <desc>, log in via POST /api/v5/login,
# then POST /api/v5/api_key with that token to mint a persistent key/secret pair.
EMQX_API_URL=http://emqx:18083/api/v5
EMQX_API_KEY=your_emqx_api_key_here
EMQX_API_SECRET=your_emqx_api_secret_here
